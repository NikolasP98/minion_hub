---
phase: 01-tab-layout-and-save-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/utils/config-schema.ts
  - src/lib/utils/config-schema.test.ts
  - src/lib/components/settings/SettingsTabBar.svelte
  - src/lib/components/settings/SettingsSkeleton.svelte
  - src/routes/settings/+page.svelte
autonomous: true
requirements: [LAYOUT-01, LAYOUT-05, PLSH-01, PLSH-03]

must_haves:
  truths:
    - "User sees 6 horizontal tabs (AI, Agents, Comms, Security, System, Appearance) below the Topbar"
    - "User can switch tabs without losing scroll position or input state in any tab"
    - "User sees card-shaped skeleton placeholders with shimmer animation while config loads"
    - "Tab labels render immediately (hardcoded); only content area shows skeletons"
    - "Appearance tab works fully offline when gateway is disconnected"
    - "Gateway config tabs show 'Not connected to gateway' message when disconnected"
    - "Sidebar and splitter are completely removed"
  artifacts:
    - path: "src/lib/utils/config-schema.ts"
      provides: "TAB_MAPPING constant and getGroupsForTab() function"
      contains: "TAB_MAPPING"
    - path: "src/lib/utils/config-schema.test.ts"
      provides: "Unit tests for tab mapping logic"
      contains: "getGroupsForTab"
    - path: "src/lib/components/settings/SettingsTabBar.svelte"
      provides: "Horizontal tab strip component with 6 tabs"
      min_lines: 30
    - path: "src/lib/components/settings/SettingsSkeleton.svelte"
      provides: "Loading skeleton cards with CSS shimmer animation"
      min_lines: 20
    - path: "src/routes/settings/+page.svelte"
      provides: "Rewritten settings page with tab bar and CSS visibility panels"
      min_lines: 100
  key_links:
    - from: "src/routes/settings/+page.svelte"
      to: "src/lib/utils/config-schema.ts"
      via: "import TAB_MAPPING and getGroupsForTab"
      pattern: "getGroupsForTab|TAB_MAPPING"
    - from: "src/routes/settings/+page.svelte"
      to: "src/lib/components/settings/SettingsTabBar.svelte"
      via: "SettingsTabBar component rendered below Topbar"
      pattern: "SettingsTabBar"
    - from: "src/routes/settings/+page.svelte"
      to: "CSS visibility toggling"
      via: "visibility: hidden + position: absolute on inactive panels"
      pattern: "visibility.*hidden|position.*absolute"
---

<objective>
Replace the sidebar/splitter settings navigation with a horizontal 6-tab layout and implement CSS visibility toggling to preserve state across tab switches. Add loading skeletons for initial config load.

Purpose: Establishes the structural foundation that all subsequent phases (cards, widgets, search, wizard) mount inside. Fixes the DOM destruction bug (LAYOUT-05) that currently loses input state on section switch.
Output: Working tabbed settings page with skeleton loading, no sidebar, state-preserving tab switches.
</objective>

<execution_context>
@/home/nikolas/.claude/get-shit-done/workflows/execute-plan.md
@/home/nikolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/routes/settings/+page.svelte
@src/lib/utils/config-schema.ts
@src/lib/state/config.svelte.ts
@src/lib/components/config/ConfigSaveBar.svelte
@src/lib/components/config/ConfigSection.svelte
@src/lib/components/settings/SettingsSidebar.svelte
@src/lib/components/Topbar.svelte
@src/lib/components/Splitter.svelte
@src/lib/state/connection.svelte.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/utils/config-schema.ts:
```typescript
export const META_GROUPS: { id: string; label: string; minOrder: number; maxOrder: number }[];
export function getMetaGroupId(order: number): string;
export function extractGroups(schema, uiHints): ConfigGroup[];
export function computeDirtyPaths(original, current): Set<string>;
export function computeDeepDirtyPaths(original, current, prefix?): Set<string>;
```

From src/lib/state/config.svelte.ts:
```typescript
export const configState = $state({ loading, loaded, loadError, schema, uiHints, original, current, baseHash, saving, saveError, lastSavedAt, version, configPath, issues, warnings });
export const isDirty = { get value(): boolean };
export const groups = { get value(): ConfigGroup[] };
export function loadConfig(): Promise<void>;
export function save(): Promise<boolean>;
export function discard(): void;
```

From src/lib/state/connection.svelte.ts:
```typescript
export const conn = $state({ connected: boolean, connecting: boolean, closed: boolean, particleHue: string });
```

From src/lib/types/config.ts:
```typescript
export interface ConfigGroup { id: string; label: string; order: number; fields: ConfigGroupField[] }
export interface ConfigGroupField { key: string; path: string; schema: JsonSchemaNode; hint: ConfigUiHint }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TAB_MAPPING and getGroupsForTab to config-schema.ts</name>
  <files>src/lib/utils/config-schema.ts, src/lib/utils/config-schema.test.ts</files>
  <behavior>
    - getGroupsForTab('ai') returns groups whose meta-group IDs are ['ai'] (order 40-79: agents, tools, bindings, audio, models)
    - getGroupsForTab('agents') returns groups from 'automation' meta-group (order 80-129: messages, commands, session, cron, hooks, ui) MINUS any security carve-outs
    - getGroupsForTab('comms') returns groups from 'comms' meta-group (order 130-199: browser, talk, channels)
    - getGroupsForTab('security') returns security-related groups carved out from other meta-groups (at minimum: session, commands -- determine exact set from GROUP_ORDER keys)
    - getGroupsForTab('system') returns groups from 'setup', 'system', and 'extensions' meta-groups (order 0-39, 200-499, 500+) MINUS security carve-outs
    - getGroupsForTab('appearance') returns empty array (appearance is local-only, not gateway config groups)
    - Every group ID in GROUP_ORDER appears in exactly one tab
    - TAB_MAPPING is exported as a constant
  </behavior>
  <action>
    1. Add a `TAB_MAPPING` constant to config-schema.ts that maps each of the 6 tab IDs to either meta-group IDs or specific group IDs. The mapping is frontend-only -- do NOT modify META_GROUPS or GROUP_ORDER.

    2. For the Security tab carve-out: inspect GROUP_ORDER and identify security-relevant groups. At minimum, `session` (order 90) and `commands` (order 85) are security-related. Create a `SECURITY_GROUP_IDS` set that overrides default meta-group placement for these groups. The Security tab gets these groups; the Agents tab (which covers the 'automation' meta-group) excludes them.

    3. Add a `getGroupsForTab(tabId: string, allGroups: ConfigGroup[]): ConfigGroup[]` function that:
       - Takes a tab ID and the full list of extracted groups
       - Returns the subset of groups belonging to that tab
       - Handles the security carve-out: groups in SECURITY_GROUP_IDS go to 'security' tab regardless of their meta-group
       - Returns empty array for 'appearance' tab (local-only settings)
       - Sorts results by group order

    4. Also export a `TABS` constant array with the 6 tab definitions: `{ id, label, icon }` where icon is the lucide icon name string (not the component -- that's for the Svelte component to resolve). Order: ai, agents, comms, security, system, appearance.

    5. Create/update `src/lib/utils/config-schema.test.ts` with tests covering all the behaviors listed above. Use vitest. Mock the groups array with representative test data covering all order ranges.

    Important: Do NOT change existing META_GROUPS order ranges or GROUP_ORDER values. The tab mapping is a layer on top.
  </action>
  <verify>
    <automated>cd /home/nikolas/Documents/CODE/AI/minion_hub && bun run vitest run src/lib/utils/config-schema.test.ts -x</automated>
  </verify>
  <done>TAB_MAPPING and getGroupsForTab exported from config-schema.ts. All tests pass. Every GROUP_ORDER key maps to exactly one tab. Security carve-out correctly redirects session/commands away from the Agents tab.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite settings page with tab bar, CSS visibility panels, and skeletons</name>
  <files>src/lib/components/settings/SettingsTabBar.svelte, src/lib/components/settings/SettingsSkeleton.svelte, src/routes/settings/+page.svelte</files>
  <action>
    **SettingsTabBar.svelte:**
    Create a horizontal tab strip component that sits below the Topbar.
    - 6 tabs: AI, Agents, Comms, Security, System, Appearance
    - Icons from lucide-svelte: Brain, Bot, Radio, Shield, Server, Palette
    - Desktop (lg+): icon + label. Below lg: icon only (responsive via `hidden lg:inline` on label span)
    - Tab style: match existing Minion Hub theming using CSS variables. Use underline-style active indicator (2px accent bottom border) or pill-style (bg-bg3 on active) -- choose what looks best with existing Topbar nav style
    - Props: `activeTab: string`, `onselect: (id: string) => void`
    - Use `role="tablist"` on container, `role="tab"` + `aria-selected` on buttons
    - Full-width horizontal strip with `border-b border-border`
    - URL-persist active tab via the existing `?s=` search param pattern (but the param now stores tab ID instead of section ID)

    **SettingsSkeleton.svelte:**
    Create a loading skeleton component showing card-shaped placeholders.
    - 4-5 skeleton cards with shimmer animation (CSS-only, left-to-right sweep)
    - Each card: fake header bar (short line) + 3-4 line placeholders of varying widths
    - Use CSS variables: `var(--color-bg3)` for base, `var(--color-border)` for highlight
    - Use `@keyframes shimmer` with `background-size: 200% 100%` and `background-position` animation
    - Wrapped in `max-w-2xl mx-auto` to match content area width
    - Anti-flash: use deferred display approach -- don't show skeleton for the first 150ms, then show if still loading. This prevents flash on fast connections. Implement with a local `$state` boolean + `setTimeout` in `onMount`.

    **+page.svelte rewrite:**
    This is a significant rewrite of the settings page. Key changes:

    1. **Remove:** Splitter, SettingsSidebar imports and usage. Remove the `Section` type and `SECTION_META` constant.

    2. **Tab state:** Change `activeSection` to read tab ID from `?s=` param (default: 'appearance'). Replace `selectSection` with `selectTab`.

    3. **CSS visibility panels:** Replace the `{#key activeSection}` block with a structure that renders ALL tab panels simultaneously. Each panel wrapped in a div with conditional classes:
       - Active: `visibility: visible; position: relative; height: auto; overflow: visible`
       - Inactive: `visibility: hidden; position: absolute; height: 0; overflow: hidden; width: 100%`
       - Use Svelte class directives or style directives (style:visibility, style:position, etc.)
       - Each tab panel gets its own `overflow-y: auto` scrollable container to preserve per-tab scroll position

    4. **Tab panel contents:**
       - **AI, Agents, Comms, Security, System tabs:** Use `getGroupsForTab(tabId, groups.value)` to get relevant groups. Show the same config section rendering (ConfigSection components in a scrollable list). When gateway disconnected: show "Not connected to gateway -- connect via the System tab or select a host from the topbar" message (styled like existing `config_noServer` pattern). When loading: show SettingsSkeleton. When loaded: show config groups.
       - **Appearance tab:** Keep existing appearance content (logo presets, theme, accent, pattern, sparkline, language). Works fully offline -- no gateway connection needed.
       - **Team, Bindings, Gateways:** These existing sections now live within their respective tabs. Team -> Security tab. Bindings -> Agents tab (per CONTEXT.md). Gateways -> System tab. Render these components alongside the config groups in their respective tab panels (TeamTab below security config groups, BindingsTab below agents config groups, GatewaySettings below system config groups).

    5. **Config loading:** The `$effect` for loading config should trigger when ANY gateway config tab is active (not just when entering a config section). Load once on first gateway tab visit.

    6. **Expanded groups:** Keep the `expandedIds` SvelteSet and auto-expand logic, but scope it per-tab if feasible (or keep global -- Claude's discretion).

    7. **ConfigSaveBar:** Keep the existing ConfigSaveBar at the bottom of the page (below all panels), visible whenever dirty/saving/error. It should NOT be inside individual tab panels -- it spans all tabs.

    8. **Page header:** Remove the per-section header (icon + title + description). The tab bar itself provides context. Optionally keep a minimal header for the active tab if it looks better.

    9. **Layout structure:**
       ```
       <div class="flex flex-col h-screen">
         <Topbar />
         <SettingsTabBar {activeTab} onselect={selectTab} />
         <div class="flex-1 min-h-0 relative">
           {#each tabs}
             <div class="tab-panel" ...visibility styles>
               <!-- tab content with own overflow-y: auto -->
             </div>
           {/each}
         </div>
         {#if isDirty.value || configState.saving || configState.saveError}
           <ConfigSaveBar />
         {/if}
       </div>
       ```

    Use `import * as m from "$lib/paraglide/messages"` for any user-facing strings. If message keys don't exist for new strings, use English literals as fallbacks (i18n keys can be added later).
  </action>
  <verify>
    <automated>cd /home/nikolas/Documents/CODE/AI/minion_hub && bun run check 2>&1 | tail -20</automated>
  </verify>
  <done>Settings page renders 6 tabs below Topbar. Sidebar and splitter are gone. Switching tabs preserves scroll position and input state (CSS visibility). Skeleton cards with shimmer show during initial config load (with anti-flash delay). Appearance tab works when disconnected. Config tabs show disconnect message. Team renders in Security tab, Bindings in Agents tab, Gateways in System tab. ConfigSaveBar visible at bottom when dirty. Type-check passes.</done>
</task>

</tasks>

<verification>
1. `bun run vitest run src/lib/utils/config-schema.test.ts -x` -- tab mapping tests pass
2. `bun run check` -- no type errors
3. Manual: Navigate to /settings, verify 6 tabs visible, switch between tabs, confirm state preserved
4. Manual: Disconnect gateway, verify config tabs show disconnect message, appearance tab still works
5. Manual: Connect to gateway, verify skeleton shows briefly then config loads
</verification>

<success_criteria>
- Settings page has 6 horizontal tabs replacing the sidebar
- Tab switching preserves all input state and scroll positions (no DOM destruction)
- Skeleton loading cards appear during initial config load with shimmer animation
- Appearance tab works fully offline
- Config tabs show clear disconnect message when gateway not connected
- Team, Bindings, and Gateways sections are integrated into their respective tabs
- All existing functionality preserved (config viewing, expanding groups, etc.)
- Type-check and tab mapping tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-tab-layout-and-save-infrastructure/01-01-SUMMARY.md`
</output>
