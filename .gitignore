node_modules
.svelte-kit
build
.vercel
*.db
/data/
.env
.env.*
!.env.example
!.env.defaults
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.playwright-mcp/
.serena/
*.png
# Tauri ships app icons as PNG; whitelist that subtree so they get tracked
!src-tauri/icons/*.png
.worktrees/

# Tauri build artifacts
src-tauri/target/
src-tauri/gen/

# Electrobun desktop build output (shell archived; rule kept harmless)
desktop-build/

# Dev tool directories
.claude/
.dmux/
.dmux-hooks/
.planning/
.superpowers/

# Reports
posthog-setup-report.md
playwright-report/
test-results/

.infisical.json
infisical-dev.sh
.paperclip-mock.local.ts
.paperclip-mock-probe.local.ts

# Migration artifacts (one-time Turso→Supabase dumps — may contain real data)
.migration-backup/

# bun is the package manager; npm lockfile is redundant
package-lock.json

# SDD process artifacts (subagent-driven-development reports)
sdd/
